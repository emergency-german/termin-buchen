name: Cleanup Successful CodeQL Runs

on:
  schedule:
    - cron: '0 * * * *' # jede Stunde
  workflow_dispatch: # manuell auslösbar

permissions:
  contents: read

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Delete successful workflow runs
        env:
          GITHUB_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          OWNER: emergency-german
          REPO: termin-buchen
          WORKFLOW_NAME: "CodeQL"
        run: |
          echo "Fetching workflow ID..."
          WORKFLOW_ID=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
            "https://api.github.com/repos/$OWNER/$REPO/actions/workflows" \
            | jq -r ".workflows[] | select(.name==\"$WORKFLOW_NAME\") | .id")
          
          if [ -z "$WORKFLOW_ID" ]; then
            echo "Workflow not found!"
            exit 1
          fi
          
          echo "Fetching successful runs..."
          RUNS=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
            "https://api.github.com/repos/$OWNER/$REPO/actions/workflows/$WORKFLOW_ID/runs?status=success&per_page=100" \
            | jq -r '.workflow_runs[].id')
          
          if [ -z "$RUNS" ]; then
            echo "No successful runs found."
            exit 0
          fi
          
          for RUN_ID in $RUNS; do
            echo "Deleting run $RUN_ID..."
            curl -s -X DELETE -H "Authorization: token $GITHUB_TOKEN" \
              "https://api.github.com/repos/$OWNER/$REPO/actions/runs/$RUN_ID"
          done
          
          echo "✅ Done!"
